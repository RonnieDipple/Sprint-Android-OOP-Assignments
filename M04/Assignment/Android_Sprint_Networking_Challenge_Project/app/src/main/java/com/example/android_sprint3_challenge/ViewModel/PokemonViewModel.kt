package com.example.android_sprint3_challenge.ViewModel

import androidx.databinding.BaseObservable
import kotlinx.android.synthetic.main.activity_main.*

/*STEP 4 - Build ViewModel

Build a ViewModel for your Pokemon application. The ViewModel will expose @Bindable properties
that can be bound to the View / UI, and will handle any click interactions.
Your ViewModel should expose any data that is bound to the UI, and should implement the "Save" and "View Details"
interactions from the UI. It should also make the actual API calls to fetch Pokemon.*/

class PokemonViewModel :  BaseObservable(){






    fun onimageViewSearchIconClicked(){}
}